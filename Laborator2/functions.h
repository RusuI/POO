#pragma once
#include "Student.h"
class functions {
public:
    int Comp_mathematics(Student* first, Student* second);
	int Comp_name(Student* first, Student* second);
	int Comp_avg(Student* first, Student* second);
	int Comp_english(Student* first, Student* second);
	int Comp_history(Student* first, Student* second);
	
 };
